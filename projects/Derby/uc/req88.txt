[DERBY-924] new JDBC4 metadata API getFunctions() needs to be implemented [DERBY-925] Implement new JDBC 4 metadata API getFunctionParameters()  <p>For now I am implementing this to return empty result set so at least we re compliant, but we should be able to implement this one</p> <p>I am currently implementing this to return an empty result set so at least we re compliant, but we should be able to provide real metadata here.</p> 