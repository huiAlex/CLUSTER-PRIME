[GROOVY-2470] Add support for coercing strings to enum classes with  as  keyword <p>It would be cool if the  as  keyword could be used to coerce strings to enum values. Since this currently throws an exception, adding this functionality should have no impact on existing code.</p>