[GROOVY-3004] List implementation that fires PropertyChangeEvents <p>ObservableList provides similar behavior as groovy.util.ObservableMap to List. It will also fire events whenever elements are added, removed, cleared and multi added/removed (via allAll,removeAll,retainAll).</p>